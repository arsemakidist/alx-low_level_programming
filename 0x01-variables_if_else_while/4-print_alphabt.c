#include <stdio.h>
/**
* main -Entry point
*
* Return: Always 0 (Success/Currect)
*/
int main(void)
{
char alphabet;
for (alphabet = 'a'; alphabet <= 'z'; alphabet++)
{
if (alphabet == 'q')
continue;
else is (alphabet == 'e')
continue;
putchar(alphabet);
}
putchar('\n);
return (0)
}
